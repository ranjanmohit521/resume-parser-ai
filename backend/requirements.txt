# Core API
fastapi
uvicorn[standard]
python-multipart
aiofiles

# NLP (pure Python — works on Python 3.14)
nltk

# PDF / Document extraction
pdfminer.six
python-docx
PyPDF2

# Image OCR
pytesseract
Pillow

# AI (optional — requires GEMINI_API_KEY env variable)
google-generativeai

# Env config
python-dotenv

# ─────────────────────────────────────────────────
# spaCy is optional but RECOMMENDED for better NER.
# It requires Python <= 3.12 and a C compiler.
# If you have Python 3.8-3.12 install with:
#   pip install spacy
#   python -m spacy download en_core_web_sm
# ─────────────────────────────────────────────────
